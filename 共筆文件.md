# 409 共同筆記
## 目錄

## JS
重啟開發區 key npm run dev
![image](https://hackmd.io/_uploads/ryhboCpRyl.png)
![image](https://hackmd.io/_uploads/S1W4jAp01x.png)

關掉開發區 在 右邊和昨天key ctal+c



## MongoDB
串公司資料寫法
```
{
  $lookup: {
    from: "AA06",
    localField: "_a",
    foreignField: "_a",
    as: "company"
  }
}
```
日期格式 擷取 年-月-日
```
{
  欄位代碼: {
    $dateToString: {
      date: "欄位代碼",
      format: "%Y-%m-%d",
      timezone: "Asia/Taipei"
    }
  },
},

```
資料為空值 要用ifNull指定 Null 要呈現什麼數字
舉例 如果 資料為空值 要呈現0
```
 欄位代碼:
        { $ifNull:[
          "MongoDB欄位代碼",0]
        },
```

排序寫法
```
{
        $sort:
          {
            欄位代碼: 1  /1 是正序 -1是逆序
          }
      }
```
要有製表人
```
contractorId: ctx.state.user.name,
```
把查詢區間的日期 放進去報表
```
searchDateFromTo: dates.欄位代碼,
```
報表查詢條件為日期起迄
``` let $and = [
      { _id: { $ne: null } }
    ]
    let dates = {
      報表查詢欄位代碼: ""
    }

    for (let [date, path] of Object.entries({
      報表查詢欄位代碼: "MongoDB的欄位代碼"
    })) {
      let 報表查詢欄位代碼 = filters.data[date]

      if (!isEmpty(報表查詢欄位代碼)) {
        const from = new Date(報表查詢欄位代碼[0])
        const to = new Date(報表查詢欄位代碼[1])

        dates[date] = `${from.toLocaleDateString("zh-tw")}~${to.toLocaleDateString("zh-tw")}`

        $and.push({
          [path]: {
            $gte: from,
            $lte: to
          }
        })
      }
    }
```
報表查詢條件資料引用
```
 for (let [field, path] of Object.entries({
      報表查詢欄位: "MongoDB的欄位代碼",
    })) {
      let value = filters.data[field]?.value

      if (!isEmpty(value)) {
        $and.push({
          [path]: { $in: value }  //多選要用in
        })
      }
    }
    
       if (!isEmpty(value)) {
    //       $and.push({
    //         [path]: value  //單選寫這樣
```
最後 要呼叫程式要寫
```
    const returnValue = await database.collection(pages.頁面代碼).aggregate(aggregates).toArray()

    return { returnValue }

```
串聯出退單
```
 {
  $unionWith: {  
   coll: "BB03",//串聯出退單
   pipeline: [
     {
       $addFields:
       {"data.shippingDate": {$ifNull:["$data.returnDate","data.shippingDate"]},  
       //若 returnDate 有值，設為 shippingDate，否則保留原 shippingDate
         
        "data.standardMoneySubtotal":   //把金額變成負數
         {$multiply: [   //乘法
             {$toDouble:
                "$data.standardMoneySubtotal"
                    },
                    -1
                  ]
                },
```
欄位如果是陣列取值得寫法 用$first
```
欄位代碼: {
            $first: "Mongo欄位代碼"
          },
```

## Jasper
顯示顯數點兩位+四捨五入
```
new DecimalFormat("#,##0.00").format(欄位代碼)

new DecimalFormat("#,##0").format(Double.parseDouble($F{quantity}))
```